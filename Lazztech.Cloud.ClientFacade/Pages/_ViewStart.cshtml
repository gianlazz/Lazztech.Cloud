@{ 
    if (User.IsInRole("Admin"))
    {
        Layout = "_Layout_Admin";
    }
    else if (this.User.Identity.IsAuthenticated)
    {
        Layout = "_Layout_Admin";
    }
    else
    {
        Layout = "_Layout";
    }
}