@page
@model Lazztech.Cloud.ClientFacade.Pages.Marketing.FocusGroups.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FocusGroup[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FocusGroup[0].DateStarted)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.FocusGroup) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateStarted)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.FocusGroupId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.FocusGroupId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.FocusGroupId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
