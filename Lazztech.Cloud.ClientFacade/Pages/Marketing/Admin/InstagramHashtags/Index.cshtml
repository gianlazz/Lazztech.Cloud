@page
@model Lazztech.Cloud.ClientFacade.Pages.Marketing.Admin.InstagramHashtags.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.InstagramHashtag[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InstagramHashtag[0].FocusGroup)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.InstagramHashtag) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FocusGroup.FocusGroupId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.InstagramHashtagId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.InstagramHashtagId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.InstagramHashtagId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
